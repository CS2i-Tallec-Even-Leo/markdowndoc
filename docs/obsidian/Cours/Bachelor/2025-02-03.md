- Monter une VM
	- Nom de la vm prénom.nom-nomdelaVM
	- Carte réseau
	- SSH
- Identifiant par défaut : 
	- Login : debian
	- Mot de passe `secret`
- Changer le mot de passe :
	- Se connecter en tant que `debian`
	- Commande `passwd`

Vérifier la configuration :

```Shell
# On liste les containers Docker
docker ps

# Version de git
git --version

# Make

make --version 
sudo apt install make

``` 


# Configuration Git

1. Si nécessaire : créer un compte Github
2. Créer une organisation **Github**